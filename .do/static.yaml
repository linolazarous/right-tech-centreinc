name: right-tech-frontend
static_sites:
- name: web
  github:
    dir: frontend
    branch: master
  source_dir: /  # Build from the frontend root
  build_command: npm install && npm run build
  output_dir: build  # react-scripts builds to 'build', not 'public'
  routes:
  - path: /
