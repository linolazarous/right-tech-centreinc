name: right-tech-frontend
static_sites:
- name: web
  github:
    dir: frontend
    branch: master
  runtime: nodejs
  build_command: npm install --include=dev && npm run build
  run_command: npm run serve  # <-- CHANGE to use the serve script
  output_dir: public
  routes:
  - path: /
