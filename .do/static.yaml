name: right-tech-frontend
static_sites:
- name: web
  github:
    dir: frontend
    branch: master
  build_command: npm install --include=dev && npm run build
  output_dir: build  # ← Change from 'public' to 'build'
  routes:
  - path: /
