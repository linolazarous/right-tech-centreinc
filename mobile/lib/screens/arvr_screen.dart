import 'package:flutter/material.dart';

class ARVRScreen extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return Scaffold(
            appBar: AppBar(
                title: Text('AR/VR Integration'),
            ),
            body: Center(
                child: Text('Experience immersive learning with AR/VR content.'),
            ),
        );
    }
}